[project]
requires-python = "~=3.11.0"
name = "bagtester"
version = "0.2.0"

[tool.black]
line-length = 120

[tool.isort]
profile = "black"

[tool.pytest]
testpaths = ["inttests"]
pythonpath = ["src"]
